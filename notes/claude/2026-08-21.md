# Session du 2026-08-21 — vérification visuelle du panneau Pin (label/description)

Reprise après la session du 2026-08-20 (item 5 de la roadmap, éditeur de
structure de puce — panneau label/description par Pin repositionné en fixe
bas-droite, style `inspector.rs`, committé et poussé sur
`feature/chip-structure-visual-rework` mais jamais vérifié visuellement en
wasm, session précédente coupée par contrainte horaire). Objectif de cette
session : combler ce trou de vérification avant de considérer le chantier
terminé.

## Ce qui a été fait

Aucun changement de code — session de vérification uniquement.

- Build wasm (`cargo build --target wasm32-unknown-unknown` +
  `wasm-bindgen` + copie des assets) à partir du code déjà committé sur
  `feature/chip-structure-visual-rework`.
- Test interactif via `claude-in-chrome` sur `localhost:8734` :
  - Placement d'un Corps + d'un Pin en vue puce (Projet 1).
  - Sélection du Pin → panneau confirmé **fixe en bas-droite** (plus
    flottant), style identique à `inspector.rs` (fond/bordure/taille),
    avec les légendes statiques "Nom" et "Description" au-dessus de
    chaque champ, comme prévu.
  - Saisie caractère par caractère (`key`, `type` ne fonctionne toujours
    pas dans cet outil sur ces champs) : "OUT" dans le champ Nom, Entrée
    pour défocus.
  - Clic sur le champ Description, saisie "SORTIE", Entrée pour défocus —
    les deux champs se comportent indépendamment (focus/saisie/défocus),
    aucune interférence entre eux.
  - Test de persistance : création d'un 2e projet vide (Projet 2, vue
    standard), retour sur Projet 1 — a d'abord affiché le **bug caméra
    pré-existant déjà documenté le 2026-08-20** (changement de projet
    pendant qu'on est en vue puce fait revenir en vue standard mais avec
    la caméra parquée en espace structure) ; ce n'est pas une régression
    de cette session, juste retombé dessus en testant. Bascule manuelle en
    vraie vue puce ensuite : Pin resélectionné, "OUT"/"SORTIE" tous deux
    correctement restaurés, aucun mélange avec Projet 2.
- `cargo test` relancé après coup : 32/32 OK (aucun changement de code,
  vérification de non-régression uniquement).

## État

Le panneau label/description par Pin (repositionnement fixe bas-droite +
champ Description) est maintenant **entièrement vérifié** — visuel, saisie,
persistance. Ce chantier de la roadmap (item 5, sous-chantier "rework visuel
de l'éditeur de structure") est terminé. Reste toujours en attente :
- Le bug caméra pré-existant (changement de projet en vue puce) — non
  traité, hors scope, toujours documenté dans la session du 2026-08-20.
- La note de suivi sur la réconciliation Description native (fixe) vs.
  Description de Pin custom (éditable) — pas cette session.
- Pas de PR : toujours en attente du système de réutilisation de puces
  entre projets pour livrer en un seul paquet (prochain gros chantier de
  l'item 5).

`graphify update .` non nécessaire (aucun fichier de code modifié cette
session).

## Chantier (suite, même session) — réconciliation Description native vs. Pin custom

Retour utilisateur juste après : la note de suivi laissée le 2026-08-20 était
en fait la prochaine priorité. Consigne : la Description ne doit plus être un
champ libre par Pin, mais une explication **fixe, non modifiable** valable
pour tout type de bloc de structure (Corps/Pin/Lampe), sur le même principe
que les descriptions figées des composants natifs (`inspector.rs`,
`selected_component_info`) — ex. Pin : "sert à faire une entrée ou une sortie
de puce." Clarifié avant de coder : le panneau doit maintenant s'ouvrir pour
n'importe quel bloc sélectionné (Corps/Pin/Lampe), pas seulement un Pin ; le
champ Nom éditable reste réservé au Pin (seul type qui en a besoin pour le
futur lien avec le circuit intérieur).

### Ce qui a été fait

- **`src/editor/chip_structure.rs`** :
  - Nouvelle fonction `structure_block_description(kind) -> &'static str`
    (juste après `StructureBlockKind`) donnant le texte fixe par type :
    Corps ("bloc de structure qui forme la silhouette extérieure de la
    puce"), Pin ("sert à faire une entrée ou une sortie de puce"), Lampe
    ("bloc indicateur qui reflète l'état d'un signal du circuit
    intérieur").
  - Suppression complète de `StructurePinDescription` (composant),
    `StructurePinDescriptionFocus` (resource), `StructurePinDescriptionField`/
    `StructurePinDescriptionFieldText` (composants), et des 4 systèmes
    d'édition associés (`handle_structure_pin_description_field_click`,
    `handle_structure_pin_description_typing`,
    `sync_structure_pin_description_field_text`,
    `sync_structure_pin_description_field_border`) — plus besoin, la
    description n'est plus une donnée par entité.
  - `spawn_structure_block` perd son paramètre `initial_description`.
  - Panneau (renommé `StructurePinLabelPanel` → `StructureBlockPanel`,
    `spawn_structure_pin_label_panel` → `spawn_structure_block_panel`,
    `sync_structure_pin_label_panel` → `sync_structure_block_panel` — le
    panneau n'est plus spécifique au Pin) : la section Nom (légende +
    champ + suggestions) est regroupée dans un nouveau conteneur
    `StructurePinNameSection` dont le `Display` est basculé selon le type
    sélectionné ; le champ Description devient un `Text` en lecture seule
    (`StructureDescriptionText`) rempli par `structure_block_description`.
    `sync_structure_block_panel` s'ouvre désormais pour tout bloc
    sélectionné (pas seulement Pin) et affiche/masque la section Nom en
    conséquence.
- **`src/editor/project.rs`** : `SavedStructureBlock` perd `description`
  (`label` reste, toujours Pin-only) ; `ViewSwitchState` perd
  `pin_description_focus` ; signature de `switch_to_project` simplifiée en
  conséquence.
- **`src/editor/sidebar.rs`** : requête `structure_blocks` de
  `handle_project_selection` simplifiée (retrait de
  `Option<&StructurePinDescription>`).
- **`src/editor/plugin.rs`** : imports/registration mis à jour (retrait des
  4 systèmes de description, retrait de `StructurePinDescriptionFocus` de
  `init_resource`, renommage des fonctions du panneau).

### Vérifications

- `cargo check`/`clippy --all-targets`/`fmt --check`/`test` (32/32) : OK.
- `graphify update .` : commande introuvable dans cet environnement (Bash et
  PowerShell), n'a pas pu être exécutée — signalé, pas bloquant.
- wasm + `claude-in-chrome` sur `localhost:8734` (nouveau build à partir du
  code modifié) :
  - Corps sélectionné → panneau avec seulement "Description" (pas de
    section Nom), texte fixe correct.
  - Pin sélectionné → panneau avec section Nom (champ éditable, vide par
    défaut) **et** Description fixe, texte fixe correct.
  - Lampe sélectionnée → panneau avec seulement "Description", texte fixe
    correct.
  - Bascule Lampe → Pin re-sélectionné : la section Nom réapparaît
    correctement (pas de état résiduel entre sélections de types
    différents).

### État

Committé pas encore fait à ce stade de la session — voir suite ci-dessous si
applicable. Ce sous-chantier réconcilie la note de suivi laissée le
2026-08-20 ; reste toujours en attente : le bug caméra pré-existant
(changement de projet en vue puce), et la PR (bloquée sur le système de
réutilisation de puces entre projets).

## Chantier (suite, même session) — le label de connexion s'étend à la Lampe

Retour utilisateur juste après : la Lampe (vue puce), comme un Pin, doit
pouvoir se connecter au label d'un pin de la vue standard — c'est-à-dire
porter le même champ Nom/label de connexion qu'un Pin, pas seulement Corps
en étant exclu.

### Ce qui a été fait

- **`src/editor/chip_structure.rs`** :
  - `spawn_structure_block` : la branche `StructureBlockKind::Lamp` gagne
    elle aussi un `StructurePinLabel(initial_label)`, comme la branche
    `Pin` (seul `Body` en reste dépourvu).
  - Doc de `StructurePinLabel` mise à jour : attaché à Pin **et** Lamp
    (plus seulement Pin), Corps exclu puisqu'il n'a rien à connecter.
  - Section Nom du panneau (`StructurePinNameSection`) : condition
    d'affichage élargie de `StructureBlockKind::Pin` à
    `StructureBlockKind::Pin | StructureBlockKind::Lamp` dans
    `sync_structure_block_panel`.
  - Les 4 systèmes d'édition/suggestion du label
    (`handle_structure_pin_label_typing`,
    `sync_structure_pin_label_field_text`,
    `sync_structure_pin_label_suggestions`,
    `handle_structure_pin_label_suggestion_click`) : filtre élargi de
    même, donc une Lampe peut désormais taper/afficher son propre label et
    apparaît dans/consulte le même pool de suggestions qu'un Pin.
  - Placeholder du champ renommé de "Label du pin" à "Label de connexion"
    (générique, puisqu'il s'applique maintenant aussi à la Lampe).
- **`src/editor/project.rs`** : doc de `SavedStructureBlock.label` mise à
  jour (Pin **ou** Lamp, plus seulement Pin) — pas de changement
  fonctionnel, la query captait déjà `Option<&StructurePinLabel>`
  génériquement.

### Vérifications

- `cargo check`/`clippy --all-targets`/`fmt --check`/`test` (32/32) : OK.
- wasm + `claude-in-chrome` sur `localhost:8734` :
  - Lampe sélectionnée → champ Nom ("Label de connexion") désormais visible
    en plus de la Description fixe, comme pour un Pin.
  - Saisie "LED" dans le Nom de la Lampe, Entrée pour défocus.
  - Pin placé et sélectionné séparément → suggestion "LED" (venant de la
    Lampe) proposée dans sa liste ; clic dessus remplit correctement le
    champ Nom du Pin avec "LED" — confirme que le pool de suggestions est
    bien partagé entre Pin et Lampe.

### État

Committé (`80fc7ac` feat, `b10101f` docs) et poussé sur
`feature/chip-structure-visual-rework`, sur demande explicite de
l'utilisateur ("on vas commit ça").

## Chantier (suite, même session) — composant Pin intérieur + lien entre les deux vues

Demande utilisateur : passer au chantier "lien entre les deux vues" et
"ajout dans un autre projet" de l'item 5. Avant de coder, lecture de la spec
Notion "Composants Modulaires & Réutilisables" (Structure/Pin/Lumière) +
clarifications avec l'utilisateur :
- La "Lumière" de la spec (indicateur porté par un pin, référençant en
  interne une Lampe/LED) diverge de ce qui existe déjà (bloc `Lampe` de
  structure autonome) — signalé à l'utilisateur, gardé tel quel sans
  reconciliation (pas bloquant, comportement cohérent).
- Décidé avec l'utilisateur : le lien se fait via un **nouveau composant
  natif "Pin"**, plaçable dans le circuit intérieur (comme un Switch/une
  Lampe), **électriquement actif** (câblable, participe à la résolution de
  net) et visuellement distinct ("bloc de plastique de pin de carte
  électronique"). Le lien structure ↔ intérieur se fait par **égalité de
  label** (même système que `StructurePinLabel` déjà posé).
- Renommage demandé : "Vue standard" → **"Vue circuit intérieur"**, "Vue
  puce" → **"Vue structure"** (aligne enfin le libellé UI sur le nom déjà
  utilisé en interne dans le code).

Passé en mode plan (`EnterPlanMode`/`ExitPlanMode`) vu l'ampleur — 2 agents
Explore en parallèle (simulation/spawn, éditeur/HUD/inspector) pour poser
l'architecture avant de coder. Plan approuvé, implémenté en une passe.

### Ce qui a été fait

- **`src/simulation/components.rs`** : nouveau marker `PinHeader` (comme
  `Lamp`) — doc précise qu'il est électriquement passif-sink pour l'instant
  (un seul pin enfant `PinRole::Input`), la distinction Entrée/Sortie étant
  reportée au chantier de réutilisation de puces entre projets (pas
  observable tant que ce système n'existe pas).
- **`src/editor/spawn.rs`** : `spawn_pin_header` (calqué sur `spawn_lamp`,
  réutilise `StructurePinLabel` importé de `chip_structure` — même
  composant de donnée que les Pin/Lampe de structure, pas de duplication) ;
  arm `ToolKind::Pin` ajouté à `spawn_placement_preview`.
- **`assets/appearances/pin_header.json`** : nouvel asset 16×16 (distinct de
  `pin.json`, le socket générique) — corps "plastique noir" avec bevel +
  accent métal sur le bord côté pin, pour lire comme un connecteur PCB
  plutôt qu'un simple point.
- **`src/constants.rs`** : `COLOR_PIN_HEADER` (placeholder pendant le
  chargement async de l'appearance).
- **`src/editor/resources.rs`** : `ToolKind::Pin` ; `placement.rs` (Digit7 +
  `place_tool`) ; `hud.rs` (7ᵉ bouton toolbar, renommage des deux libellés de
  vue + élargissement du bouton pour "Vue circuit intérieur").
- **`src/editor/pin_header.rs`** (nouveau fichier) : panneau dédié bas-droite
  (même style que `chip_structure::StructureBlockPanel`) avec champ Nom
  éditable + suggestions + Description fixe + indicateur "Lié" — 6 systèmes
  focus/typing/sync-text/border/suggestions/click, dupliqués et adaptés
  depuis le patron structure (`Selected`/`ArmedTool` du circuit intérieur au
  lieu de `SelectedStructureBlock`/`ArmedStructureTool`).
- **`src/editor/chip_structure.rs`** : `sync_structure_pin_label_suggestions`
  simplifié (retrait du filtre `StructureBlockKind`, redondant) pour que le
  pool de suggestions inclue aussi les labels des `PinHeader` intérieurs —
  un seul pool partagé entre les deux vues. Ajout de l'indicateur "Lié"
  (même logique) sur le panneau structure.
- **`src/editor/resources.rs`/`edit_mode.rs`/`project.rs`** :
  `PinHeaderLabelFocus` ajouté à `TransientEditorState` et remis à zéro dans
  `reset_transient_editor_state` (mode toggle + changement de projet).
- **`src/editor/project.rs`** : nouveau variant `SavedEntity::Pin { cell,
  rotation, label }` ; nouveau `SystemParam` `CircuitQueries` regroupant
  gates/switches/lamps/pin_headers/cables (nécessaire : `sidebar::handle_project_selection`
  était déjà à exactement 16 paramètres système, la limite Bevy — regrouper
  était obligatoire pour ajouter la query PinHeader, même stratégie que
  `StructureCustomization`/`ViewSwitchState`).
- **`src/editor/plugin.rs`** : imports, `init_resource`, systèmes Startup +
  les deux blocs Update (gated Standard pour l'input, ungated pour le sync).

### Vérifications

- `cargo check`/`clippy --all-targets`/`fmt --check`/`test` (32/32) : OK,
  compile sans erreur dès la première passe.
- wasm + `claude-in-chrome` sur `localhost:8734` :
  - Libellés "Vue structure"/"Vue circuit intérieur" corrects, pas de wrap.
  - 7ᵉ bouton "7: Pin" présent et fonctionnel, rendu "bloc plastique"
    distinct du socket générique confirmé au zoom.
  - Câblage Switch → Pin par drag : câble posé correctement, **signal
    propagé réellement** (câble + socket du Pin passent à l'orange/HIGH
    quand le Switch est activé en mode Interaction) — confirme que
    `PinHeader` est un vrai nœud de résolution de net, pas juste un
    placeholder visuel.
  - Panneau dédié : Nom éditable + Description fixe corrects à la sélection
    d'un Pin intérieur.
  - Lien cross-vue : label "LED" tapé sur le Pin intérieur → apparaît en
    suggestion sur un Pin de structure ; une fois les deux labels
    identiques, indicateur "Lié" affiché **des deux côtés**.
  - Persistance : Switch + Pin (avec son label) restaurés correctement après
    changement de projet aller-retour, sans mélange — testé en évitant
    délibérément le bug caméra pré-existant (changement de projet pendant
    qu'on est en Vue structure, déjà documenté 2026-08-20/21 comme hors
    scope) en restant en Vue circuit intérieur pendant le changement de
    projet.
  - Retombé une fois de plus sur le bug caméra pré-existant en testant un
    enchaînement différent (créer un projet pendant qu'on est en Vue
    structure) — confirmé toujours présent, toujours hors scope, pas une
    régression de ce chantier.

### État

Committé (`4542962` feat, `bf3d3d7` docs) et poussé sur
`feature/chip-structure-visual-rework`. Le composant Pin intérieur
électriquement actif + le lien par label entre les deux vues sont
fonctionnels et vérifiés. Reste pour un chantier futur : la distinction
Entrée/Sortie du Pin intérieur (deviendra pertinente avec le placement de
puces dans d'autres projets), le bug caméra pré-existant.

## Chantier (suite, même session) — placement visuel d'une puce dans un autre projet

Demande utilisateur : poursuivre avec "l'ajout de puce dans un projet"
(item 5 de la roadmap). Clarifié avec l'utilisateur avant de coder :
- **Premier jalon = placement visuel seulement** (pas de simulation live à
  travers la puce placée pour l'instant).
- **UX** : nouvel outil toolbar "Puce" ouvrant une liste des autres projets
  existants ; cliquer un projet l'arme comme n'importe quel autre outil.
- **Publication** : aucune étape séparée — tout projet existant est déjà
  disponible comme puce (exclusion du projet actif lui-même, et de tout
  projet sans structure).
- La puce placée est **figée au moment du placement** (snapshot), pas liée
  en live au projet source.

Passé en mode plan (`EnterPlanMode`/`ExitPlanMode`) vu l'ampleur — pas de
nouvelle exploration par agent nécessaire, connaissance du code déjà fraîche
depuis le chantier précédent de cette session. Plan approuvé, implémenté en
une passe.

### Ce qui a été fait

- **`src/editor/project.rs`** : nouvelle méthode publique
  `ProjectLibrary::chip_blueprint(id)` (retourne un `ChipBlueprint` — nom
  affiché, teinte du Corps, liste `(cellule, kind)` — sans exposer le champ
  privé `data` brut) ; `None` si le projet n'a jamais été visité ou n'a
  aucun bloc de structure. Nouveau variant `SavedEntity::Chip` (figé :
  source, nom, couleur, blocs) et capture/respawn dans `switch_to_project` ;
  `CircuitQueries` gagne `chip_instances`.
- **`src/editor/chip_instance.rs`** (nouveau fichier) : composant
  `ChipInstance` (source, nom affiché, couleur, blocs figés) ;
  `spawn_chip_instance` (rend chaque bloc comme enfant purement visuel,
  offset local = cellule locale de structure — donc l'ancre de placement est
  la cellule que le créateur de la puce avait traitée comme son propre
  (0,0), pas nécessairement un bloc visible) ; `spawn_chip_instance_preview`
  pour le fantôme de placement ; sélecteur "Puce" (bouton toggle + liste de
  projets, même principe collapse/expand que la sidebar Projets),
  `StandardEditorUi`-tagué (une puce ne se place que côté circuit
  intérieur).
- **`src/editor/resources.rs`** : `ToolKind::Chip(ProjectId)`.
- **`src/editor/interaction.rs`/`placement.rs`** : `handle_left_click_start`
  gagne `Res<ProjectLibrary>`, `place_tool` résout `ToolKind::Chip` via
  `chip_blueprint` puis appelle `spawn_chip_instance` (no-op silencieux si
  `None`, même comportement qu'un clic sur cellule occupée).
- **`src/editor/preview.rs`** : `ToolKind::Chip` intercepté avant
  `spawn::spawn_placement_preview` (qui n'a pas accès à `ProjectLibrary`) —
  appelle `chip_instance::spawn_chip_instance_preview` directement.
- **`src/editor/inspector.rs`** : `selected_component_info` généralisé de
  `(&'static str, &'static str)` à `(String, String)` (un nom de puce est
  une valeur runtime, pas un littéral fixe) ; nouveau branch pour
  `ChipInstance` (nom dynamique + description fixe générique).
- **`src/editor/edit_mode.rs`** : nouveau type `FootprintQuery` (alias,
  évite le lint clippy `type_complexity` répété 3 fois) ; `draw_entity_outline`
  calcule la boîte de surlignage d'une puce placée depuis l'étendue min/max
  de ses blocs (`chip_instance_bounds`) plutôt que la case fixe 2 lignes des
  portes AND/OR — une puce a une forme arbitraire.
- **`src/editor/plugin.rs`** : imports, `init_resource`, systèmes Startup +
  les deux blocs Update (gated Standard pour l'input, ungated pour le sync).

### Vérifications

- `cargo check`/`clippy --all-targets`/`fmt --check`/`test` (32/32) : OK.
  Un aller-retour nécessaire : `spawn.rs::spawn_placement_preview` avait un
  match non-exhaustif sur `ToolKind` (nouveau variant `Chip`) — corrigé avec
  un no-op arm (le vrai traitement se fait dans `preview.rs` avant d'y
  arriver) ; 3 lints `type_complexity` sur les query génériques (corrigés
  avec des alias de type) ; un `#[allow(...)]` dupliqué par erreur.
- wasm + `claude-in-chrome` sur `localhost:8734` :
  - Construit une structure (Corps + Pin) dans Projet 1, nommée "AND".
  - Créé Projet 2, ouvert le sélecteur "Puce" : "AND" y apparaît (Projet 1
    exclu puisqu'actif à ce moment n'aurait pas dû l'être — en fait c'est
    Projet 2 qui était actif et exclu de sa propre liste, comportement
    correct).
  - Placement : rendu correct (Corps + Pin visibles, jambe de connexion).
  - Sélection : contour de sélection couvrant **toute** la puce (pas
    seulement la cellule d'ancre, invisible dans ce cas car aucun bloc
    n'était à la cellule locale (0,0) du design d'origine — comportement
    attendu, documenté).
  - Panneau d'info : Nom = "AND", Description fixe correcte, Créateur/Date
    placeholders, bouton Loupe inerte.
  - Rotation : la puce entière (Corps + Pin) pivote ensemble, contour de
    sélection suit correctement.
  - Persistance : changement de projet aller-retour, puce restaurée
    identique (position, rotation, blocs, nom) sans mélange.
  - Suppression : fonctionne.
  - Correction en cours de route : le bouton "Puce" n'était pas tagué
    `StandardEditorUi`, donc visible (mais mort, ses systèmes de clic étant
    gated à `ProjectView::Standard`) même en Vue structure — corrigé.
  - Retombé de nouveau sur le bug caméra pré-existant en créant un nouveau
    projet depuis la Vue structure — confirmé toujours présent, toujours
    hors scope.

### État

Pas encore committé. Placement visuel de puces entre projets fonctionnel et
vérifié de bout en bout (placement, sélection, rotation, suppression,
persistance, panneau d'info). Reste pour un chantier futur : simulation live
à travers une puce placée (ses pins portant un vrai signal vers/depuis le
circuit hôte), et toujours le bug caméra pré-existant.
